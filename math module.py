import math
print(dir(math))

from math import sqrt
print(sqrt(int(input("enter the no. : "))))

from math import *
print(sqrt(int(input("enter the no. : "))))

# both the functions sqrt and "*" does some work when putted with import module